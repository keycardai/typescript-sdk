name: Bump Package Version

on:
  workflow_call:
    inputs:
      package_name:
        required: true
        type: string
        description: "Name of the package to bump (e.g., keycardai-oauth)"
      package_dir:
        required: true
        type: string
        description: "Directory of the package (e.g., packages/oauth)"

jobs:
  bump:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          # Checkout with PAT so git operations (fetch, reset) use an identity
          # that can create PRs and whose tag pushes fire downstream workflows.
          token: ${{ secrets.RELEASE_GITHUB_PAT }}
          fetch-depth: 0

      - name: Install commitizen
        run: pip install commitizen

      - name: Bump version for ${{ inputs.package_name }}
        env:
          GH_TOKEN: ${{ secrets.RELEASE_GITHUB_PAT }}
        run: |
          python scripts/bump_package.py "${{ inputs.package_name }}" "${{ inputs.package_dir }}"
